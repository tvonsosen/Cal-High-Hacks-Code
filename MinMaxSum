integerList = list(map(int, input("List 5 integers with spaces in between them: ").split()))
def miniMaxSum(intList):
    minSum = sum(intList) - max(intList)
    maxSum = sum(intList) - min(intList)
    print(minSum,maxSum)
miniMaxSum(integerList)
